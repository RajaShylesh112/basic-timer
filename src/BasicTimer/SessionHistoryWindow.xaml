<Window x:Class="BasicTimer.SessionHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BasicTimer"
        mc:Ignorable="d"
        Title="Session History" Height="500" Width="700"
        WindowStartupLocation="CenterScreen">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Summary Stats -->
        <GroupBox Grid.Row="0" Header="Today's Summary" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="Sessions: " FontWeight="Bold"/>
                    <TextBlock Text="{Binding SessionCountToday}" />
                </StackPanel>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="Time Today: " FontWeight="Bold"/>
                    <TextBlock Text="{Binding TotalTimeToday}" />
                </StackPanel>
                
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <TextBlock Text="Time This Week: " FontWeight="Bold"/>
                    <TextBlock Text="{Binding TotalTimeThisWeek}" />
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- Session List -->
        <DataGrid Grid.Row="1" 
                  Name="SessionDataGrid"
                  ItemsSource="{Binding Sessions}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  GridLinesVisibility="Horizontal"
                  AlternatingRowBackground="#F5F5F5">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Start Time" Binding="{Binding FormattedStartTime}" Width="140"/>
                <DataGridTextColumn Header="End Time" Binding="{Binding FormattedEndTime}" Width="140"/>
                <DataGridTextColumn Header="Duration" Binding="{Binding FormattedDuration}" Width="80"/>
                <DataGridTextColumn Header="Type" Binding="{Binding TypeDescription}" Width="80"/>
                <DataGridTextColumn Header="Initial Time" Binding="{Binding InitialTime}" Width="80"/>
                <DataGridTextColumn Header="Activity" Binding="{Binding Activity}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Export Options -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,10">
            <Button Name="ExportButton" Content="Export to CSV" Width="100" Height="30" Margin="0,0,10,0" Click="ExportButton_Click"/>
            <Button Name="ClearButton" Content="Clear History" Width="100" Height="30" Margin="0,0,10,0" Click="ClearButton_Click"/>
            <TextBlock Text="Export sessions to CSV file or clear all history" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="Gray"/>
        </StackPanel>

        <!-- Close Button -->
        <Button Grid.Row="3" Content="Close" Width="80" Height="30" HorizontalAlignment="Right" Click="CloseButton_Click"/>
    </Grid>
</Window>
